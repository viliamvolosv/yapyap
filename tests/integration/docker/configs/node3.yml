node:
  name: node3
  data_dir: /data
  api_port: 8080

network:
  listen: 0.0.0.0:4001
  bootstrap:
    - /dns4/node1/tcp/4001
    - /dns4/node2/tcp/4001

crypto:
  keystore: /data/keys
  passphrase: test-pass

storage:
  path: /data/db.sqlite
